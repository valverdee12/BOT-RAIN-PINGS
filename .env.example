# Copiar a .env y rellenar
DISCORD_TOKEN=tu_token_aqui
DISCORD_CHANNEL_ID=tu_canal_id_aqui
# Modo del detector: SIMULATE (por defecto) | HTTP_POLL | WEBSOCKET
BANDIT_MODE=SIMULATE
# Si usas HTTP_POLL, define la URL de la API que devuelve eventos JSON
BANDIT_API_URL=
# Plantilla para construir la URL de Bandit Camp si el evento no incluye `url`.
# Usa placeholders: {id}, {hand}, {player}
BANDIT_URL_TEMPLATE=https://bandit.camp/hand/{id}
# Nota: la URL sólo se usará internamente para detectar/identificar el evento; no se mostrará
# en los mensajes de Discord.
# Puedes especificar solo la base si prefieres:
BANDIT_BASE_URL=https://bandit.camp/
# Opcional: para HTML_POLL (comprobar la página principal o el path del chat que contiene enlaces)
BANDIT_HTML_URL=https://bandit.camp/
# Intervalo de polling en milisegundos (por defecto 20000 = 20s)
POLL_INTERVAL_MS=20000
# Modo de renderizado completo (usa puppeteer para ejecutar JS y evitar bloqueos)
# Requiere instalar dependencias (puppeteer) y puede aumentar el uso de CPU/RAM.
# Usa BANDIT_MODE=PUPPETEER para activarlo.
BANDIT_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64)
